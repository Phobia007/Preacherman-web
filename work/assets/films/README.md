# Four showcase films

## Vessel replacement — 2026-09-18

Slot01 uses the user-supplied Preacherman-Vessel-Gallery-23s.mp4 as
vessel-gallery-23s.mp4: 23seconds,1280×800,30fps,H.264,no audio. Video is
copied without re-encoding, with fast-start MP4 metadata and derived posters.
Both appearances keep the authored video colors, with existing themed controls.
The visible Vessel film loops; theme changes preserve the source/playback time.
Offscreen, hidden or covered playback pauses. Reduced-motion users see the end
poster with explicit Play. Other films retain their existing finish/hold behavior.

The following original documentation applies to the retained older assets.

Four independently rendered Blender concept films: establish a personal workspace, exchange the engine while retaining identity/state, carry a complete asset beyond its workspace, and continue remembered history.

Each theme variant is 1280×800, 10 seconds, 600 actual rendered frames at 60 fps, H.264 without audio. The `start` WebP is the loading poster; `end` is the result poster for reduced motion. Existing outside headings carry the bilingual meaning.

Provenance: independently copied existing website Pathfinder GLB, SHA-256 36507991df9c0ca9503adf6c953e44233fe9b7525a2fba842b4e6e736aa7bfde. No desktop source or runtime resource dependency.

Editable Blender projects, scene generator, original PNG masters and verification evidence are preserved at `D:/preacherman/output/blender/web-four-films-20260916/`. `delivery.json` records final hashes and checks.

Only the visible film plays. Inactive/hidden/modal-covered films pause. Changing appearance preserves the story clock; completion holds the final frame. The small control allows explicit Pause/Play/Replay. Reduced-motion users see the result poster unless they explicitly choose Play.

The local dev server supports video byte ranges. Published files remain explicit in `scripts/frontend-assets.mjs`. Videos are copied into both build destinations; this delivery does not publish to a domain.


## State and engine decoupled — local card 02

state-engine-24s.mp4: 24 seconds, 1280×800, 30 fps, muted web export. Doorway push → actual desktop Settings → Execution Mode → Local CLI → black → approved promo source frames 801–837, then final-frame hold. Remotion composition State-Engine-Decoupled. Both website appearances use the same authored video; the existing themed chrome, pause/replay and reduced-motion behavior remain in place. Only local preview/build updated; no publication.

## Your Property — card 03, 2026-09-21

`your-property-23s.mp4` is the supplied `Preacherman-New-Scenes-Next-1080p.mp4`,
copied without re-encoding: 1920×1080, 30 fps, 704 frames, 23.466667 seconds,
H.264 with the original AAC audio retained. The website player stays muted.
SHA-256: `d4d456c20a42520975a433ad7476c93972aa9ce2c5131817396014811032db45`.

The sequence contains the approved ending transition, desktop Gallery, digital
identity title, Manage your assets, Market views and the original Next button.
Both appearances share this authored video. Contain sizing preserves its full
frame; the existing themed card and playback controls remain. Start and result
posters are extracted at 0 and 22.43 seconds, before the concluding fade to black.
The source video and both posters are included in the explicit publish allowlist.